# Core dependencies
python>=3.11
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=14.0.0

# API and HTTP
httpx>=0.24.0
requests>=2.31.0
aiohttp>=3.9.0

# Geospatial
geopandas>=0.14.0
rasterio>=1.3.0
shapely>=2.0.0
fiona>=1.9.0

# Data processing
polars>=0.19.0
scipy>=1.11.0
scikit-learn>=1.3.0

# Deep Learning (ROCm for AMD 7800XT)
# Note: Install PyTorch with ROCm separately:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm5.7
# Or use: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm6.0
# Check: https://pytorch.org/get-started/locally/ for latest ROCm version

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0
tqdm>=4.66.0

# Logging and monitoring
loguru>=0.7.0

# Testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0

